.table-actions {
    display: flex;
    flex-flow: row wrap;
    justify-content: flex-end;
    align-items: center;
    padding: 0 0 0 1.75rem;

    & > * + * {
        margin-left: 0.5rem;
    }
}

.table-notification {
    padding: 1rem;
    text-align: center;
}
