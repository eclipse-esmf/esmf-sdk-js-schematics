<%= options.fieldConfig.name %>: new FormControl({value: false, disabled: <%= options.fieldConfig.readOnlyForm %>}, [
    <% for(let validator of options.fieldConfig.validators) { %>
        <%= validator.definition %>
    <% } %>
]),