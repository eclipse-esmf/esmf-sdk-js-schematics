import {Injectable} from '@angular/core';

// TODO: It's a temporary stub for the EsmfLocalStorageService. Will be removed during the actual migration of the service.
@Injectable()
export class EsmfLocalStorageService {
  setItem(x, y) {
    console.log(x, y);
  }
}
